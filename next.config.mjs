/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["startup-template-sage.vercel.app"], // Domínio da imagem que você quer usar
  },
};

export default nextConfig;
